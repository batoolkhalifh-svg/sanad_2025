workflows:
  ios-release:
    name: Build Sanad 2025 iOS
    max_build_duration: 60
    environment:
      flutter: stable
      xcode: latest
      groups:
        - appstore_credentials
        - ios_config

    scripts:
      - name: تثبيت الحزم
        script: |
          flutter pub get

      - name: إنشاء ملف المفتاح من Base64
        script: |
          echo $APP_STORE_CONNECT_PRIVATE_KEY | base64 --decode > AuthKey.p8

      - name: بناء تطبيق iOS
        script: |
          flutter build ios --release

    artifacts:
      - build/ios/ipa/*.ipa

    publishing:
      app_store_connect:
        api_key: AuthKey.p8    # استخدم الملف اللي أنشأناه
        key_id: ${APP_STORE_CONNECT_KEY_IDENTIFIER}
        issuer_id: ${APP_STORE_CONNECT_ISSUER_ID}
