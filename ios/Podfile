# Podfile for Flutter project

platform :ios, '12.0'

use_frameworks!
use_modular_headers!

target 'Runner' do
  # لا تستخدم flutter_install_all_ios_pods
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
  end
end
